import type React from "react"
export default function AdminLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}


// This layout is used for the admin pages in the app 
// It is a simple layout that renders the children passed to it
// It is used to wrap the admin pages in the app
// It is a simple layout that does not add any additional styling or functionality
